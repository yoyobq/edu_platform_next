# 通用环境变量示例文件。
# 这里预留给后续 dev / shared 配置使用。
# 当前仓库已经启用的 E2E 专用变量，请查看 `env/.env.e2e.example`。
# development 模式的本地开发配置，请查看 `env/.env.development.example`。

# 示例：
# APP_ENV=development
# API_BASE_URL=http://localhost:3000
