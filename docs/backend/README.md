<!-- docs/backend/README.md -->

# Backend Docs

本目录用于放按需参考的后端契约文档。

当前约定：

- `schema.graphql` 属于本地参考文件
- 默认不提交到仓库
- 只有在当前任务确实依赖后端字段、类型、query、mutation 或 input 时，才按需查看相关片段
- 不建议全文阅读完整 schema

若仓库内看不到 `schema.graphql`，表示当前本地未放置，或该文件按约定未提交。
